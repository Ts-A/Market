syntax = "proto3";

service UserService {
    rpc CreateUser (UserInput) returns (User);
    rpc DeleteUser (UserId) returns (User);
    rpc EditUser (UserId) returns (User);
    rpc GetAllUsers (Empty) returns (UserList);
    rpc GetUser (UserId) returns (User);
}

message Empty {};

message User {
    string id = 1;
    string name = 2;
    string email = 3;
    string role = 4;
    string password = 5;
}

message UserInput {
    string name = 2;
    string email = 3;
    string role = 4;
    string password = 5;
}

message UserId {
    string id = 1;
}

message UserList {
    repeated User users = 1;
}

